import 'package:flutter/material.dart';
import 'app.dart';

void main() {
  // Ensure Flutter binding is initialized for web
  WidgetsFlutterBinding.ensureInitialized();

  runApp(const HybridDashboardApp());
}
